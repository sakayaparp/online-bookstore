{
  "name": "root",
  "private": true,
  "devDependencies": {
    "cypress": "8.2.0",
    "lerna": "^4.0.0"
  },
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "lerna run build",
    "lint": "lerna run lint"
  }
}
