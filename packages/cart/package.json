{
  "name": "cart",
  "private": true,
  "scripts": {
    "start:dev": "npx ts-node index.ts",
    "db:init": "sequelize-cli db:migrate:undo:all && sequelize-cli db:migrate && sequelize-cli db:seed:all",
    "test": "npx jest"
  },
  "dependencies": {
    "express": "^4.17.1",
    "sequelize": "^6.12.0-beta.3",
    "ts-jest": "^27.1.1"
  }
}
