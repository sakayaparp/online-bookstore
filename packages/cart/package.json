{
  "name": "cart",
  "private": true,
  "scripts": {
    "start:dev": "npx ts-node index.ts",
    "test": "npx jest",
    "db:init": "npx sequelize-cli db:migrate:undo:all && npx sequelize-cli db:drop && npx sequelize-cli db:create && npx sequelize-cli db:migrate && npx sequelize-cli db:seed:all"
  },
  "dependencies": {
    "express": "^4.17.1",
    "pg": "^8.7.1",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.12.0",
    "ts-jest": "^27.1.1"
  },
  "devDependencies": {
    "sequelize-cli": "^6.3.0"
  }
}
